import './Button.scss';
import {Template, config} from  './Button';
import {Meta, Preview, Story} from '@storybook/addon-docs/blocks'

<Meta
    title="Atomic/Button/Variants"
    argTypes={config.argTypes}
/>

# Default
<Preview>
    <Story name="Default" args={{
        color: 'primary',
        label: 'Bouton',
        size: 'default',
        type: 'default',
        iconPosition: 'default',
        icon: 'svg',
    }}>
        {Template.bind({})}
    </Story>
</Preview>

# Outline
<Preview>
    <Story name="Outline" args={{
        color: 'primary',
        label: 'Bouton',
        size: 'default',
        type: 'outline',
        iconPosition: 'default',
        icon: 'svg',
    }}>
        {Template.bind({})}
    </Story>
</Preview>

# Ghost
<Preview>
    <Story name="Ghost" args={{
        color: 'primary',
        label: 'Bouton',
        size: 'default',
        type: 'ghost',
        iconPosition: 'default',
        icon: 'svg',
    }}>
        {Template.bind({})}
    </Story>
</Preview>

# Link
<Preview>
    <Story name="Link" args={{
        color: 'primary',
        label: 'Bouton',
        size: 'default',
        type: 'link',
        iconPosition: 'default',
        icon: 'svg',
    }}>
        {Template.bind({})}
    </Story>
</Preview>

# With SVG Icon
<Preview>
    <Story name="WithSvgIcon" args={{
        color: 'primary',
        label: 'Bouton',
        size: 'default',
        type: 'default',
        iconPosition: 'left',
        icon: 'svg',
    }}>
        {Template.bind({})}
    </Story>
</Preview>

# With Css.gg Icon
<Preview>
    <Story name="WithCssggIcon" args={{
        color: 'primary',
        label: 'Bouton',
        size: 'default',
        type: 'default',
        iconPosition: 'left',
        icon: 'cssgg',
    }}>
        {Template.bind({})}
    </Story>
</Preview>

# Disabled
<Preview>
    <Story name="Disabled" args={{
        color: 'primary',
        label: 'Bouton',
        size: 'default',
        type: 'ghost',
        iconPosition: 'default',
        icon: 'svg',
        disabled: true,
    }}>
        {Template.bind({})}
    </Story>
</Preview>
